[user]
	name = Lance Ball
	email = lball@redhat.com
[color]
	diff = auto
	status = auto
	branch = auto
[apply]
	whitespace = nowarn

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[github]
	user = lance
	token = AINTNOTHINGBUTATHANG
[alias]
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
  which-branch = !git rev-parse --symbolic-full-name --abbrev-ref $1
[core]
	excludesfile = /Users/lanceball/.gitignore
