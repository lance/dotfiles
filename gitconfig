[user]
	name = Lance
	email = lance@shovelpunks.com
[color]
	diff = auto
	status = auto
	branch = auto
[apply]
	whitespace = nowarn

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan

[github]
	user = lance
	token = SOMETHINGUNGUESSABLE
[alias]
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
  frebase = !git fetch && git rebase origin
